// =============================================================================
// ⚠️ TEMPORARILY DISABLED — video preview is off.
//
// The original media_kit-based implementation was removed from this file
// (incl. all imports). To restore, recover it from git history at the
// commit just before the disablement (search for `video_preview.dart`);
// you'll also need to re-add the media_kit / media_kit_video /
// media_kit_libs_macos_video / media_kit_libs_windows_video entries to
// pubspec.yaml, and re-introduce the `VideoPreview(videoBytes: ...)` call
// in `lib/screens/file_browser_screen.dart#onVideoSaved`.
// =============================================================================